#include<iostream>
using namespace std;
int main() {
	int a,b,z;
	cout << "Enter a number: ";
	cin >> a;
	z = a; 
	b = a * 10;
	
	for (int j = 1; j <= 20; j++)
	{
		for (int i = z; i <= b; i+=a)
			cout << i << "\t";
		z = z + (10*a);
		b = b + (10*a);
		cout << endl;
		}
}
